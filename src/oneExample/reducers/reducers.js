import actionTypes from '../actions/actionTypes'

let init={
  datas:[],
};

export default function todos(state=init,action){
  switch (action.type) {
    // case value:
      
    //   break;
  
    default:
      return state;
  }
}